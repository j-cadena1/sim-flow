# Sim-Flow Environment Configuration
# ====================================
# Copy to .env and fill in your values. NEVER commit .env to git!

# ==========================================
# ENVIRONMENT
# ==========================================
# Set to 'development' for local testing (non-secure cookies for HTTP)
# Set to 'production' for deployment (secure cookies, requires HTTPS)
# NODE_ENV=development

# ==========================================
# REQUIRED
# ==========================================

# Database password - generate with: openssl rand -base64 24
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# SSO encryption key - generate with: openssl rand -base64 32
# Required to encrypt SSO client secrets in the database
SSO_ENCRYPTION_KEY=CHANGE_ME_USE_OPENSSL_RAND_BASE64_32

# ==========================================
# PRODUCTION SETTINGS
# ==========================================

# Your public URL (required for SSO callbacks and CORS)
# CORS_ORIGIN=https://simflow.yourdomain.com

# ==========================================
# SSO - MICROSOFT ENTRA ID (optional)
# ==========================================
# Can also be configured via Settings UI (qAdmin only).
# Env vars take effect if database config is missing/disabled.
# When configured via env, the Settings SSO tab is hidden.

# SSO_TENANT_ID=your-azure-tenant-id
# SSO_CLIENT_ID=your-azure-client-id
# SSO_CLIENT_SECRET=your-azure-client-secret
# SSO_REDIRECT_URI=https://simflow.yourdomain.com/api/auth/sso/callback
